# Включить заданное число D в массив A(N), упорядоченный по возрастанию, с сохранением упорядоченности
n = int(input("Введите количество элементов массива: "))
A = []
print("Введите элементы массива (в порядке возрастания):")
for i in range(n):
    A.append(int(input(f"Элемент {i + 1}: ")))

D = int(input("Введите число D для вставки в массив: "))
for i in range(len(A)):
    if D <= A[i]:
        A.insert(i, D)
        break
else:
    A.append(D)
print("Обновленный массив:", A)
